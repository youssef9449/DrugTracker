node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Lockfiles are intentionally not committed — this repo has never tracked
# one (developers run `npm install` / `bun install` locally). Keep it that
# way so dependency resolution stays driven by package.json semver ranges.
package-lock.json
bun.lock
bun.lockb

# Capacitor native project (generated by `npx cap add android`).
# Users create it on their own machine to build the APK.
android/
ios/
node_modules/
