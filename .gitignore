node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Capacitor native project (generated by `npx cap add android`).
# Users create it on their own machine to build the APK.
android/
ios/
node_modules/

# Signing keystores — never commit these.
*.keystore
*.jks
*.p12

# Phase 2 JVM unit-test module build output
native-android/jvm-tests/.gradle/
native-android/jvm-tests/build/
